# Required — Mongo connection string
MONGODB_URI=mongodb://localhost:27017/promptvault

# Optional — override the database name (default: promptvault)
MONGODB_DB=promptvault

# Optional — port the API listens on (Railway injects this automatically)
PORT=8080
